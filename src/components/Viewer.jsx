import styled from 'styled-components';

const StyleContainer = styled.div`
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, #242424, lightblue);
`;

function Viewer() {
  return <StyleContainer></StyleContainer>;
}

export default Viewer;
